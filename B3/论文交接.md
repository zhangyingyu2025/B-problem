# B3 最终交付：E13-R（2026-09-13）

本交付供组员撰写B3论文，算法与三局正式测试使用的E13-R一致。最终交付指代码和测试证据已整理，不代表论文已经完成或官方已确认全清比例。

## 正式结果，可用于论文表格

|案例编码|清除干扰源个数|平均定位清除时间（秒/源）|程序运行时间（秒，接口重建）|
|---|---:|---:|---:|
|845S-PDZN-BJES-56PB|14|220.319037|2.391|
|9HR8-M9ZV-Q9EY-XF5S|10|313.262567|1.952|
|V9S6-H7S5-WUA4-RBNE|14|229.255392|2.079|

三局累计清除38个源，累计虚拟时间9426.667687秒；加权平均为248.070202秒/源。三局均值的算术平均为254.278999秒/源，两者不能混用。不要把目标220秒/源写成已达到，也不要把演练或人工构造结果标成正式测试。

平均定位清除时间采用正常退出时累计虚拟时间除以成功清除的不同频道数，包括移动、测量、切换和失败清除。程序运行时间是enter/exit响应时间戳之差；现有截图未给出独立精确到毫秒的官方运行耗时，定稿时保留此来源说明或用官方明确数据及来源核验。正式案例不公开真实总源数，不能声称清除率100%。三局均正常退出且日志成本审计通过。

## 组员阅读顺序

1. 当前策略入口：`experimental/E13/solver.py`；路由：`experimental/E13/event_route.py`。
2. 数学说明：`experimental/E13/model_notes.md`。其中旧变体名和历史研究记录仅作背景，最终执行选项为R。`b1plus.py`为B3专用区域约束，B1本体未据此修改。
3. 正式总表：`结果/e13_formal/summary/table.csv`；各局`evidence-*`目录含SVG轨迹、动作CSV和审计结果。
4. 各局`simulator_logs`含原名`.jlog`及结束页截图；`actions.jsonl`为客户端原始日志。不要改写、改名官方日志。
5. 演练结果在`结果/e13_rehearsal`，与正式分开。旧离线批次已按要求清理，其历史数字不能替代本次正式结果。

轨迹上的清除点是机器狗执行清除的坐标，不是源的精确真值。不能将MEC中心首次尝试写成必然成功：当前算法包含明确区分的有界风险尝试与失败恢复。E11继承链仍属于现行代码依赖，勿自行删除。

## 克隆后离线重新生成表格和图（不连接模拟器）

在仓库根目录运行，Python标准库即可：

```powershell
python -B -X utf8 tools/test_evidence/export.py --manifest B3/final_formal_manifest.json --output B3/结果/paper-rebuilt
```

输出目录必须不存在。`final_formal_manifest.json`使用相对路径，支持其他电脑。原始运行记录中的绝对路径仅用于保留历史来源，组员重建请使用此manifest，不能直接复用其中本机路径。三局数值及SVG已在临时目录重建一致。

正式测试操作脚本为`tools/test_evidence/run_b3_formal.ps1`。撰写论文和画图不需要再次执行正式测试。

完整核验说明及相对路径证据清单见仓库`求解/reproducibility_report.md`和`求解/artifact_manifest.json`。所有仍存在的原运行provenance所列源码与原哈希一致；其额外列出的3个未启用候选模块已清理，不属于R依赖。旧100场原始数据已删除，不能声称本次再次通过该回归。
