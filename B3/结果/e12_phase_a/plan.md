# E12 第一阶段执行范围

使用 mathskills 的 plan-and-execution 与 validation-and-robustness；用户已明确授权此阶段实现。

1. 基线：以本地工作树及日志为准，审计全部 E11 启动和现有历史结果；跑 B1/B2/B3 回归。
2. 冻结：独立提交 E11 与源文件哈希清单，保留本地标签；不修改 E11 文件或旧实验结果，不推送。
3. 接口：E12 继承被冻结的 E11 引擎；默认六外点直接调用父类原方法，只增加 coverage template 和 selector hook。
4. 七外点：证明半径1005米的覆盖，避免总站数7的硬编码；保留同一联合调度、恢复、网格和协议代码。
5. 验收：固定种子24494904、另两个小型随机案例、boundary/cluster/origin及全near场景；逐动作严格比较原始E11与默认/显式六点分支。允许仅删除请求ID、会话ID和墙钟字段，位置和虚拟时间不设容差。七点分支做覆盖单测和有限离线冒烟，不做性能排名。
6. 所有差异或失败保留；完成全回归、原件哈希核验后向用户汇报。

停止范围：不运行development200、locked validation200或性能调参，不接E12官方模拟器，不启动formal。旧fixed12/unseen80只核验历史记录，不能调参。
