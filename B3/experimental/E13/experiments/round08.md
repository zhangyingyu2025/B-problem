# Round 08: scenario route, before performance observations

Control J; candidate L changes only the next-action routing score. Same B1+, R<=80 explicit risky-clear policy, six fixed cover stations, inherited transit observations and finish recovery.

Hypothesis: online first-action regret is reduced by conditioning future tasks on silence and enforcing discovery/localization precedence in forecast routes. Eight deterministic scenarios; 1024-point area quadrature; 13/20 channel occupancy prior is an explicit heuristic assumption, not a physical guarantee or estimate from this batch. Forecasts cannot access case seeds, transport or true sources. Candidate actions <=10. Three NN tails and precedence-safe 2-opt are score approximations, not optimality bounds.

New random seeds 52000700–52000719, E11/J/L paired, fixed_field error. Four workers, existing 120 second per-run guard. Do not change parameters after inspecting this set; retain null and failed trials. L must all-clear and improve paired movement and time/tails over J before further testing. No coverage deformation before <=11.5 km/run. No official/formal run in this round.
