# B3最终交付复现审计（2026-09-13）

范围：仅B3 E13-R三局正式证据及后处理；不是全项目或论文编译认证。环境：Windows、Python 3.12.1、标准库。未连接模拟器，未重复任何正式测试。

## 已通过

- 三局原始actions SHA256与总表登记一致，正常退出、状态连续、成功频道去重、时间成本核验通过。
- 清除数、每源时间、接口重建现实耗时从原日志重新计算，与登记数值完全一致。
- 使用B3/final_formal_manifest.json相对路径，在新临时目录重建三局表格和SVG，数值完全一致，三张SVG逐字节一致。
- 三局均保存与案例编码匹配的原名jlog和结束截图；只记录哈希，未解密或改动内容。
- 当前仍存在的原provenance所列源码LF哈希全部匹配。清理的event_route_cap.py、solver_cap.py、solver_diag.py为原清单额外包含的未启用候选；R依赖保留。
- 9项后处理/正式入口模拟测试、6项几何测试、2项路由测试通过，共17项。正式入口测试使用mock，没有HTTP连接。

## 限制及下一步

- blocking：本次表图离线重建无阻塞；旧100场原始回归已按用户要求删除，相关历史审计不能本地完整重跑。这不是已通过的回归。
- evidence-dependent：总源数不公开，不能报告正式清除率；程序现实时间为接口时间戳重建值，若论文要求官方精确耗时，应核对明确的官方记录，不能用整数倒计时截图虚构毫秒精度。
- presentation-only：论文尚未编写，未验证排版、引用和编译。数学开发记录含旧变体背景，组员应以当前R代码及B3/论文交接.md为边界。
- 原始运行JSON中的绝对路径作为历史记录保留；跨电脑重建应使用新增相对路径manifest。输出目录需新建，避免覆盖结果。

数值、图表到输入日志及生成命令的映射见同目录artifact_manifest.json。官方密文日志是支撑材料，不等于本工具可以独立恢复隐藏测试场景；本次复现仅重建确认动作的统计与轨迹。
