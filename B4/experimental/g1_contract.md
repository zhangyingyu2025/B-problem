# G1 联合调度开发预登记

G0正确性已通过，随机100场875.182 s/源，主要瓶颈是逐源完成导致的往返。G1仅将25点发现与已发现源的下一步动作加入同一事件队列；保留同一25点证书、SPF45比例1.30、8步fan保护、MEC≤20保证清除和同一有限fallback。

每个源只生成当前真实信息允许的下一任务：near/保证clear、锚点clear（建立20m下界）、一对fan的第一支。第一支失去信号时按同一证据执行第二支；每次得到新读数后旧任务坐标失效并重规划。不提前计划未知读数，不使用普通no_signal距离约束。候选站点全部实际扫描未知频道才积累证书。

第一版本复用只读B3 fast_open_route排序当前有限节点；尚不加入额外机会测量、mesh压缩、风险clear、方向先验。G1状态机可在每个真实动作后重规划，减少G0的强制逐源执行。全局动作上限防循环，源fan上限不变。

配对DEV：先64000000–64000019与现有G0同case、同fixed_field；通过后扩至64000000–64000199。100场已有G0可重用完全相同保存实例/误差，后100场须补G0。专项stress沿用已登记6401/6402/6403/6404万段。新结果完整保留，不把先导子集算独立确认。HOLDOUT仍锁定。

验收：全清、零保证失败、无真值访问为硬门槛；报告逐场时间差、成本分解及P95/CVaR95、N=10/11/12。若仍>500，以日志成本诊断下一因素；禁止用250场G0全清代替G1验证，也不能以一次均值下降宣称达到最终目标。
