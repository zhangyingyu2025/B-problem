# G1D 有限延期试验

G1 的 200 场全清，613.801 秒/源；每局平均31.024公里。20场动作去向分解：certificate12.22km、fan第一支9.18km、第二支3.80km、保证clear5.88km。当前任务TSP每次改变主扫方向，且先做大跨度fan未必节省总路程。

只更换调度：固定25点开放路线顺序，已发现源的下一动作若相对下一站的插入增量≤200m则允许插入，否则等路线走近；证书结束后全部解除延期，按G1有限任务排序完成。200m为本轮单一预设，不做阈值网格搜索。保留同一geometry/SPF/clear/fallback；先配对DEV20，若改善且全清才扩200。即使无效也保留日志与实现作审计，不更改G1基线。
