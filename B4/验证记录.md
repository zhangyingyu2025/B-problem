# B4 验证记录

## G0 正确性阶段（2026-09-12）

51项 B4 自动测试通过，含31项协议、7项发现证书、5项SPF、4项几何、2项无真值接口、2项端到端测试。SPF测试包含100000随机组和1350端点组合。另强制fan_limit=0验证独立有限覆盖清除；正常批次没有依赖该兜底。

|人工批次|种子|全清场数|全清源数|weighted s/源|平均移动m|兜底场数|
|---|---|---:|---:|---:|---:|---:|
|random_mixed|64000000–64000099|100/100|1297/1297|875.182|48225.693|0|
|boundary|64020000–64020049|50/50|649/649|659.751|33044.811|0|
|all_directional|64010000–64010049|50/50|652/652|912.531|50128.847|0|
|random_mixed / endpoints|64040000–64040049|50/50|650/650|836.714|45985.319|0|

250场共3248源全清；SPF双分支无信号和保证清除失败均为0。批次没有读取HOLDOUT。20场先导测试是random100的子集，不另计独立验证场数。所有数字来自人工环境，不代表官方演练成绩。完整成本、尾部、N分层见 `g0_validation.json`，逐场动作与结果在忽略的 `B4/results/`。

性能不合格：随机均值875.182，主要为移动。G0每个证书站点扫描后把已发现源全部处理完，导致长距离往返；下一步只改事件联合调度，不一起改网格、SPF比例和风险清除阈值。

既有功能检查：B1 27项通过、B2 31项通过、B3/Q1502的24项测试通过。旧R等价测试的入口因E12旧清单仍记录修订前的B2哈希而报错；错误与此前 `62f628d` 的B2修订一致，B4没有改B2。保留该旧清单后，`B4/code/verify_previous.py` 核验B4开始时65份既有Python源码哈希，再实际比较原100场R的完整动作序列与指标，全部相同、全清。

可复现命令：

```powershell
python -B -X utf8 -m unittest discover -s B4/tests -v
python -B -X utf8 B4/code/verify_previous.py
python -B -X utf8 -u B4/code/run_offline.py --start 64000000 --count 100 --variant G0 --pattern random_mixed --workers 4 --output B4/results/NEW_g0_random100.json
```

新输出名称不可覆盖旧批次；更改路径不更改数据身份。正常全清时按源数加权时间；存在失败的批次不发布可用于排名的weighted指标。原始失败行仍保留。不对外声称已经通过网络原始报文、正式环境或350–380性能验收。
