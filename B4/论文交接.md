# B4正式交付（2026-09-13）

使用当前独立包`B4.code.solver.B4Solver`。运行记录称V2；源码说明另有冻结坐标的跨平台修正记录，准确版本以source_manifest.json及各局session.json源码哈希为准。三局正式测试全部使用同一组源码，交付源码与原运行哈希一致。此包不是原始G52-T160检查点，不能将二者实验数据混用。

|正式案例编码|清除源数|平均定位清除时间（秒/源）|程序运行时间（秒，接口重建）|
|---|---:|---:|---:|
|5PYH-SS62-7UT9-GQXV|14|384.220205|6.661|
|8D8G-SXAP-SFEF-ZASW|12|507.887521|7.073|
|6TUJ-XFUA-68AG-BR6X|12|486.368126|7.288|

累计清除38个源，累计虚拟时间17310.150649秒，加权平均455.530280秒/源；三局等权平均459.491951秒/源。平均定位清除时间包含所有移动、测量、切换和清除尝试成本。三局均正常完成、日志审计通过，保证清除及有限覆盖相关失效计数为0。正式案例总源数未公开，不可宣称官方清除率100%。程序现实时间来自enter/exit响应时间戳之差，不能冒充另行核验过的官方内部精确耗时。

## 材料位置

- 当前求解器及独立依赖：`code/`、`source_manifest.json`。
- 正式累计表：`results/v2_formal/summary/table.csv`。
- 各局`run-*`内：`actions.jsonl`、`result.json`、`session.json`。
- 各局`evidence-*`内：SVG轨迹、动作CSV与逐局审计数据。
- 各局`simulator_logs`内：原名加密`.jlog`及结束截图。保留原文件名与字节内容。
- DEV50人工构造回归输入与参考：`results/dev50_cases.json`、`results/dev50_reference.json`；不可标为正式结果。
- 正式与演练操作：仓库`tools/test_evidence/B4操作说明.md`。此次上传正式证据，不将本地演练批次混入正式表。

轨迹上的清除点为机器狗操作坐标，不是干扰源真实位置。

## 克隆后离线复现（不启动测试）

在仓库根目录，Python标准库即可运行：

```powershell
python -B -X utf8 -m unittest discover -s B4/tests -q
python -B -X utf8 tools/test_evidence/export.py --manifest B4/final_formal_manifest.json --output B4/results/paper-rebuilt
```

输出目录必须不存在。manifest使用相对路径，适用于组员电脑。原始JSON内本机绝对路径保留为历史记录，重建请使用此manifest。三局统计已重算一致，三张SVG已在临时目录逐字节重建一致；核验信息见formal_delivery_audit.json。

本次验证4项独立包测试及11项工具测试。VERIFICATION.md中的完整DEV50报告来自包内已有验证记录，本次交付未重新运行全部50场，更未重跑正式测试。写作应以当前源码、正式原始记录和明确的来源说明为依据。
